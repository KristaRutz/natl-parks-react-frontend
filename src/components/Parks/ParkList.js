import React from "react";
// import ParkListItem from './ParkListItem';
// import PropTypes from 'prop-types';

function ParkList(props) {

  // const {} = props;

  return(
    <React.Fragment>
      {/* {Object.values(parkList).map((park) => {
        return <Park/>
      })} */}
    </React.Fragment>
  )
}

export default ParkList;